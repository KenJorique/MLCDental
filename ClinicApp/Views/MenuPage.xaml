<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClinicApp.Views.MenuPage"
             Title="Menu">

	<VerticalStackLayout Padding="20" Spacing="10">

		<Frame Padding="15" HasShadow="True">
			<Frame.GestureRecognizers>
				<TapGestureRecognizer Command="{Binding GoToServicesCommand}" />
			</Frame.GestureRecognizers>
			<Label Text="Service &amp; Pricing" FontSize="16" />
		</Frame>

		<Frame Padding="15" HasShadow="True">
			<Frame.GestureRecognizers>
				<TapGestureRecognizer Command="{Binding GoToUsersCommand}" />
			</Frame.GestureRecognizers>
			<Label Text="User Management" FontSize="16" />
		</Frame>

		<Frame Padding="15" HasShadow="True">
			<Frame.GestureRecognizers>
				<TapGestureRecognizer Command="{Binding GoToSupplyCommand}" />
			</Frame.GestureRecognizers>
			<Label Text="Medical Supplies" FontSize="16" />
		</Frame>

        <!-- Add inside your MenuPage content -->
        <Frame Margin="12" Padding="14" CornerRadius="12"
       BorderColor="#E0E0E0" BackgroundColor="White">
            <Grid ColumnDefinitions="*,Auto">
                <VerticalStackLayout Grid.Column="0" Spacing="2">
                    <Label Text="Google Account" FontSize="14"
                   FontAttributes="Bold" TextColor="#333"/>
                    <Label Text="{Binding GoogleEmail}"
                   FontSize="12" TextColor="#888"/>
                </VerticalStackLayout>
                <Button Grid.Column="1"
                Text="{Binding GoogleButtonText}"
                BackgroundColor="#4285F4"
                TextColor="White" CornerRadius="8"
                FontSize="12" HeightRequest="36"
                Command="{Binding GoogleSignInCommand}"/>
            </Grid>
        </Frame>

       

    </VerticalStackLayout>

</ContentPage>
